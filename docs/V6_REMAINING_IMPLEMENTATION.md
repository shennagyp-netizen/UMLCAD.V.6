# V6 Remaining Implementation

The remaining V6 work is executed as independent kernel contracts and native conformance slices:

- NURBS/freeform surface realization;
- general sweep/pipe;
- offsets/healing;
- deterministic tessellation;
- STEP/IGES exchange;
- final robustness, topology-history, assembly, motion, and engineering-analysis foundations.

Each slice must preserve the UMLCAD semantic authority boundary and pass the existing regression gate before the next slice is declared complete.
