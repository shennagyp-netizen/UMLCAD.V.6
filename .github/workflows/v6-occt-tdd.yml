name: V6 OCCT TDD

on:
  push:
    branches:
      - v6-occt-tdd
  pull_request:
    branches:
      - main
      - v6-occt-tdd

jobs:
  rust-occt:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install OCCT and build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            cmake \
            pkg-config \
            libtbb-dev \
            libocct-foundation-dev \
            libocct-modeling-data-dev \
            libocct-modeling-algorithms-dev \
            libocct-data-exchange-dev \
            libocct-visualization-dev

      - name: Verify OCCT development files
        run: |
          test -f /usr/include/opencascade/NCollection_AliasedArray.hxx
          test -f /usr/include/opencascade/IGESControl_Controller.hxx
          test -f /usr/include/opencascade/STEPControl_Reader.hxx
          test -e /usr/lib/x86_64-linux-gnu/libtbb.so
          test -e /usr/lib/x86_64-linux-gnu/libTKIGES.so
          test -e /usr/lib/x86_64-linux-gnu/libTKSTEP.so
          dpkg-query -W libocct-foundation-dev libocct-data-exchange-dev libocct-visualization-dev libtbb-dev

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.1
          components: rustfmt, clippy

      - name: Format gate
        working-directory: rust
        run: cargo fmt --all

      - name: Test gate (debug)
        working-directory: rust
        run: cargo test --workspace --all-targets

      - name: Test gate (release)
        working-directory: rust
        run: cargo test --workspace --all-targets --release

      - name: Clippy gate
        working-directory: rust
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Kernel format gate
        working-directory: kernel_rust
        run: cargo fmt --all

      - name: Kernel test gate (debug)
        working-directory: kernel_rust
        run: cargo test --all-targets

      - name: Kernel test gate (release)
        working-directory: kernel_rust
        run: cargo test --all-targets --release

      - name: Kernel clippy source gate
        working-directory: kernel_rust
        run: cargo clippy --lib --bins -- -D warnings

      - name: Kernel clippy test gate
        working-directory: kernel_rust
        run: cargo clippy --tests -- -D warnings -A clippy::useless_vec -A clippy::bool_assert_comparison
